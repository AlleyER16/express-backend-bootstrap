import { User } from "./user.model";
import { UserSession } from "./userSession.model";

import { Setting, SettingsGroup } from "./settings.model";

export default [User, UserSession, Setting, SettingsGroup];
