import AppController from "./app.controller";

import AuthController from "./auth.controller";
import AccountController from "./account.controller";

export default [AppController, AuthController, AccountController];
